pipeline {
    agent any
    stages {
        stage('Get') {
            steps {
               sh "mvn dependency:get -DrepoUrl=http://localhost:8081/repository/maven-snapshots/ Dartifact=org.springframework.samples:sprint-petclinic:LATEST:jar -Ddest=component.jar"
            }
        }
    }
}
