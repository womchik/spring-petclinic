pipeline {
    agent any
    stages {
        stage('Get') {
            steps {
               sh "mvn dependency:get -DrepoUrl=http://localhost:8081 Dartifact=org.springframework.samples:sprint-petclinic:SNAPSHOT:jar -Ddest=component.jar"
            }
        }
    }
}
